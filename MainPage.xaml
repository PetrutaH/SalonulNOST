<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SalonulNOST.MainPage"
             Title="Home">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Image Source="salon.jpg" HeightRequest="250" Aspect="AspectFit" SemanticProperties.Description="Salon image" />
            <Label Text="Hello, bun venit la SalonulNost!" Style="{StaticResource Headline}" SemanticProperties.HeadingLevel="Level1" />
            <Label Text="Welcome to .NET Multi-platform App UI" Style="{StaticResource SubHeadline}" SemanticProperties.HeadingLevel="Level2" />

            <!-- Buton pentru Profil, vizibil doar daca utilizatorul este logat -->
            <Button Text="Go to Profile" Clicked="OnProfileButtonClicked" HorizontalOptions="Fill" IsVisible="False" x:Name="ProfileButton"/>
            <Button Text="Logout" Clicked="OnLogoutButtonClicked" HorizontalOptions="Fill" IsVisible="False" x:Name="LogoutButton"/>

           
            <!-- Buton de Count -->
            <Button x:Name="CounterBtn" Text="Click me" SemanticProperties.Hint="Counts the number of times you click" Clicked="OnCounterClicked" HorizontalOptions="Fill"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
