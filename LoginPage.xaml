<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SalonulNOST.LoginPage"
             Title="Login">

    <VerticalStackLayout Padding="30,0" Spacing="20">
        <Label Text="Login to your account" FontSize="24" HorizontalOptions="Center"/>
        <Entry x:Name="EmailEntry" Placeholder="Email" Keyboard="Email"/>
        <Entry x:Name="PasswordEntry" Placeholder="Password" IsPassword="True"/>

        <Button Text="Login" Clicked="OnLoginButtonClicked"/>

        <Button Text="Don't have an account? Register" Clicked="OnRegisterButtonClicked"/>
    </VerticalStackLayout>
</ContentPage>
